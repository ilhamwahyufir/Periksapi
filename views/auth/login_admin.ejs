<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Admin - PERIKSAPI</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #e0f7fa 0%, #f1f8e9 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .login-container {
            width: 100%;
            max-width: 750px;
            display: grid;
            grid-template-columns: 0.9fr 1.1fr;
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 40px rgba(0,0,0,0.12);
        }

        /* Left Side - Branding */
        .left-side {
            background: linear-gradient(135deg, #00695c 0%, #004d40 100%);
            padding: 25px 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .left-side::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 300px;
            height: 300px;
            background: rgba(255,255,255,0.1);
            border-radius: 50%;
        }

        .left-side::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: -10%;
            width: 250px;
            height: 250px;
            background: rgba(255,255,255,0.05);
            border-radius: 50%;
        }

        .brand-logo {
            width: 55px;
            height: 55px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            margin-bottom: 12px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.2);
            position: relative;
            z-index: 1;
        }

        .brand-name {
            font-size: 1.4rem;
            font-weight: 800;
            margin-bottom: 6px;
            position: relative;
            z-index: 1;
        }

        .brand-tagline {
            font-size: 0.7rem;
            text-align: center;
            opacity: 0.9;
            line-height: 1.3;
            position: relative;
            z-index: 1;
            max-width: 180px;
        }

        .admin-badge {
            margin-top: 15px;
            background: rgba(255,255,255,0.2);
            padding: 8px 14px;
            border-radius: 8px;
            position: relative;
            z-index: 1;
            border: 2px solid rgba(255,255,255,0.3);
        }

        .admin-badge-icon {
            font-size: 1.3rem;
            margin-bottom: 3px;
        }

        .admin-badge-text {
            font-size: 0.75rem;
            font-weight: 600;
        }

        .admin-note {
            margin-top: 10px;
            font-size: 0.65rem;
            opacity: 0.8;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        /* Right Side - Login Form */
        .right-side {
            padding: 25px 30px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .form-header {
            margin-bottom: 18px;
        }

        .form-header h2 {
            font-size: 1.25rem;
            font-weight: 800;
            color: #333;
            margin-bottom: 3px;
        }

        .form-header p {
            color: #666;
            font-size: 0.78rem;
        }

        /* Alert Error */
        .alert-error {
            background: #ffebee;
            border: 2px solid #ef5350;
            border-radius: 10px;
            padding: 10px 12px;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
            color: #c62828;
            font-weight: 500;
            font-size: 0.8rem;
        }

        .alert-icon {
            font-size: 1.1rem;
        }

        /* Security Notice */
        .security-notice {
            background: #fff3e0;
            border: 2px solid #ff9800;
            border-radius: 10px;
            padding: 10px 12px;
            margin-bottom: 16px;
            display: flex;
            align-items: start;
            gap: 8px;
            color: #e65100;
        }

        .security-icon {
            font-size: 1.1rem;
            margin-top: 2px;
        }

        .security-text {
            flex: 1;
        }

        .security-text strong {
            display: block;
            margin-bottom: 2px;
            font-size: 0.8rem;
        }

        .security-text small {
            font-size: 0.68rem;
            opacity: 0.9;
        }

        /* Form Group */
        .form-group {
            margin-bottom: 15px;
        }

        .form-label {
            font-weight: 600;
            color: #333;
            margin-bottom: 5px;
            display: block;
            font-size: 0.8rem;
        }

        .form-input {
            width: 100%;
            padding: 10px 14px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 0.85rem;
            transition: all 0.3s;
            background: #f8f9fa;
        }

        .form-input:focus {
            outline: none;
            border-color: #00695c;
            background: white;
            box-shadow: 0 0 0 3px rgba(0,105,92,0.1);
        }

        /* Submit Button */
        .btn-submit {
            width: 100%;
            padding: 11px;
            background: linear-gradient(135deg, #00695c 0%, #004d40 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 0.9rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 6px 15px rgba(0,105,92,0.3);
            margin-bottom: 12px;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0,105,92,0.4);
        }

        .btn-submit:active {
            transform: translateY(0);
        }

        /* Links */
        .form-links {
            text-align: center;
            font-size: 0.8rem;
        }

        .form-links a {
            color: #00695c;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
        }

        .form-links a:hover {
            color: #004d40;
            text-decoration: underline;
        }

        .divider {
            text-align: center;
            margin: 12px 0;
            color: #999;
            font-size: 0.75rem;
        }

        .switch-role {
            background: #f8f9fa;
            padding: 10px;
            border-radius: 10px;
            text-align: center;
            margin-top: 12px;
            border: 2px solid #e0e0e0;
            font-size: 0.8rem;
        }

        .switch-role p {
            margin: 0;
            color: #666;
        }

        .switch-role a {
            color: #00695c;
            font-weight: 600;
            text-decoration: none;
        }

        .switch-role a:hover {
            text-decoration: underline;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .login-container {
                grid-template-columns: 1fr;
                max-width: 450px;
            }

            .left-side {
                padding: 30px 25px;
            }

            .brand-logo {
                width: 60px;
                height: 60px;
                font-size: 2rem;
            }

            .brand-name {
                font-size: 1.5rem;
            }

            .brand-tagline {
                font-size: 0.8rem;
            }

            .admin-badge {
                margin-top: 20px;
                padding: 10px 15px;
            }

            .admin-badge-icon {
                font-size: 1.5rem;
            }

            .right-side {
                padding: 30px 25px;
            }

            .form-header h2 {
                font-size: 1.3rem;
            }
        }
    </style>
</head>
<body>

    <div class="login-container">
        <!-- Left Side - Branding -->
        <div class="left-side">
            <div class="brand-logo">🐄</div>
            <div class="brand-name">PERIKSAPI</div>
            <div class="brand-tagline">
                Sistem Pakar Diagnosa Penyakit Sapi Berbasis AI
            </div>
            <div class="admin-badge">
                <div class="admin-badge-icon">👨‍💼</div>
                <div class="admin-badge-text">Portal Administrator</div>
            </div>
            <div class="admin-note">
                🔒 Akses khusus untuk administrator sistem
            </div>
        </div>

        <!-- Right Side - Login Form -->
        <div class="right-side">
            <div class="form-header">
                <h2>Login Admin 🛡️</h2>
                <p>Masuk untuk mengelola sistem PERIKSAPI</p>
            </div>

            <!-- Contoh error message (uncomment untuk testing) -->
            <!-- <div class="alert-error">
                <span class="alert-icon">⚠️</span>
                <span>Email atau password salah</span>
            </div> -->

            <!-- Security Notice -->
            <div class="security-notice">
                <span class="security-icon">🔐</span>
                <div class="security-text">
                    <strong>Area Terbatas</strong>
                    <small>Halaman ini hanya untuk administrator.</small>
                </div>
            </div>

            <form action="/admin/login" method="POST">
                <!-- Email Input -->
                <div class="form-group">
                    <label class="form-label">📧 Email Admin</label>
                    <input type="email" 
                           name="email" 
                           class="form-input" 
                           placeholder="admin@periksapi.com"
                           required
                           autocomplete="email">
                </div>

                <!-- Password Input -->
                <div class="form-group">
                    <label class="form-label">🔒 Password</label>
                    <input type="password" 
                           name="password" 
                           class="form-input" 
                           placeholder="Masukkan password admin"
                           required
                           autocomplete="current-password">
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn-submit">
                    🚀 Masuk ke Dashboard Admin
                </button>

                <!-- Switch Role -->
                <div class="switch-role">
                    <p>Login sebagai user? <a href="/login">👤 Klik di sini</a></p>
                </div>

                <div class="divider">atau</div>
                
                <div class="form-links">
                    <a href="/">⬅ Kembali ke Beranda</a>
                </div>
            </form>
        </div>
    </div>

</body>
</html>